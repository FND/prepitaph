{
	"scripts": {
		"build": "node ./src/ssg/index.js",
		"rebuild": "rm -rf ./dist && npm run build",
		"test": "eslint --cache ./src && echo ✓",
		"profile": "rm -rf ./profiling && ./bin/profile"
	},
	"type": "module",
	"dependencies": {
		"commonmark": "^0.30.0",
		"lampenfieber": "^1.1.0",
		"metacolon": "^1.1.1",
		"prismjs": "^1.29.0"
	},
	"devDependencies": {
		"esbuild": "^0.17.11",
		"eslint-config-fnd": "^1.13.0"
	}
}
